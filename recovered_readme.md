# Experiment Documentation

これは櫻井くんの過去のコマンド履歴をもとに自動生成したドキュメント。やんわりと合っているが、このドキュメントで書かれているコマンドは動作しない。 
`README.md` を見よ！

## 詳細コマンド手順 (日本語)
本プロジェクトで実行された主なコマンド例:

1. conda での環境作成・切り替え  
   - 「conda create -n dcase2024」「conda activate dcase2024」などで新しい環境を作成・有効化  
   - PyTorch や pandas、librosa など必要ライブラリをインストール  

2. DCASE タスク関連スクリプトの実行  
   - 「python generate_dcase_task4_2024.py --only_strong」などで特定データの生成・前処理  
   - 「python train_pretrained.py --test_from_checkpoint ...」などで学習済みモデルの再テスト  

3. HPC 環境でのリソース管理  
   - 「nvidia-smi」「htop」「tmux」などで GPU と CPU の状態監視や複数セッション管理  

4. LogBERT を用いたログ異常検知  
   - 「python HDFS_data.py」「python main.py bgl」などでデータセットの前処理やモデル実行  
   - 「python test.py ./outputs/... ValTotalbest.pth」などで学習後の推論・評価  

これらのコマンドを組み合わせ、データ準備・学習・検証を繰り返し行うことで、実験を進めていたと推測される。

## 実験内容の詳細
### DCASE タスク
- **データ生成**: `generate_dcase_task4_2024.py` スクリプトを使用して、特定のデータセットを生成。
- **モデル学習と評価**: `train_pretrained.py` スクリプトを使用して、事前学習済みモデルを再学習および評価。

### LogBERT を用いたログ異常検知
- **データセットの前処理**: `HDFS_data.py` や `BGL_data.py` などのスクリプトを使用して、ログデータセットを前処理。
- **モデル実行と評価**: `main.py` や `test.py` スクリプトを使用して、モデルの学習と評価を実行。

### プログラムの動作
- **環境設定**: conda 環境を作成し、必要なライブラリをインストール。
- **データ生成と前処理**: スクリプトを実行してデータセットを生成および前処理。
- **モデル学習と評価**: 学習スクリプトを実行してモデルを学習し、評価スクリプトを使用してモデルの性能を評価。
- **リソース管理**: GPU や CPU の使用状況を監視し、効率的にリソースを管理。

これらの手順により、プロジェクトは音声イベント検出やログ異常検知の実験を行っていたことがわかります。
